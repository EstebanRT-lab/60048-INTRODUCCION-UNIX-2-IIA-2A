# ssh me permite conectarme a ordenadores externos, por ejemplo con este código me conecto a bandit 0

# Clear sirve para limpiar el historial 

#history sirve para ver el historial 

#pwd (print working directory) me muestra el directorio  en la que estoy trabajando en este momento 

#cd (change directory) me sirve para cambiar de directorio

#ls (list) me da una lista de comandos y directorios 
#ls --all ó ls -a muestran TODOS los directorios, son un comando extendido y un abreviado 

#los archivos y los directorios que empiezan con punto "." son ocultos
# el punto "." simboliza el directorio actual 
#El punto punto ".." simboliza a la carpeta madre o contenedora del directorio en el que estoy 

#cd / nos lleva al directorio raiz, el que contiene a todos los otros directorios 

#touch crea un nuevo programa (después podemos confirmar que se creó con ls)